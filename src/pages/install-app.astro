---
// The standalone-app install flow is now consolidated into the unified
// installation guide (a version selector at the top: NinjaTrader 8 / Tradovate /
// Ironbeam). Permanent redirect preserves SEO weight and any inbound links
// (older marketing copy, the app, emails) — the fragment lands visitors straight
// on the Tradovate/Ironbeam (standalone app) panel. Kept as an Astro page so it
// behaves identically in dev, preview, and production.
return Astro.redirect('/installation-guide#tradovate', 301);
---
